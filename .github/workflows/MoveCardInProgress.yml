name: automatically move project card when issue/PR is assigned
on:
    issues:
        types:
            - assigned
    pull_request:
        types:
            - assigned
jobs:
    moveCard:
        runs-on: ubuntu-latest
        steps:
            - name: Move Project Card
              uses: peter-evans/create-or-update-project-card@v1.1.4
              with:
                  project-name: 後端進度
                  column-name: 在做了在做了
